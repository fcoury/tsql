## Before committing changes to this repository

- You _MUST_ run `cargo fmt --all` to format the code.

## Before pushing changes to this repository

- You _MUST_ run `cargo clippy --all --all-targets -- -D warnings` to check for lints and fix them.
